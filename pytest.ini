[pytest]
# web3's legacy `web3.tools.pytest_ethereum` plugin (registered as `pytest_ethereum`)
# is dead code that imports `ethpm`/`ContractName`, both removed from modern
# eth-typing. It is unused by this repo, so we disable it to keep the suite runnable.
addopts = -p no:pytest_ethereum
testpaths = tests
